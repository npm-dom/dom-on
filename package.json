{
  "name": "dom-on",
  "version": "1.0.2",
  "description": "Alias a DOM node's .addEventListener() to .on()",
  "main": "index.js",
  "scripts": {
    "test": "standard && NODE_ENV=test node test",
    "test-cov": "standard && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "npm-dom/dom-on",
  "keywords": [
    "dom",
    "event",
    "on"
  ],
  "license": "MIT",
  "dependencies": {
    "is-dom": "^1.0.5"
  },
  "devDependencies": {
    "dom-stub": "^1.0.1",
    "tape": "^4.0.0"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
